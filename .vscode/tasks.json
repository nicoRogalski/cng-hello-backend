{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run local",
            "type": "shell",
            "command": "go run ./cmd/server/",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
        {
            "label": "build image",
            "type": "shell",
            "command": "docker build -f build/package/docker/Dockerfile -t cng-hello-backend --build-arg version=$(cat VERSION) .",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}