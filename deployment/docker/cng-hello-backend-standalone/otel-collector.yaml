extensions:

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317 # It is important that we do not use localhost

processors:
  memory_limiter:
    limit_percentage: 75
    check_interval: 5s
    spike_limit_percentage: 30
  batch:
    send_batch_size: 8192

exporters:
  jaeger:
    endpoint: jaeger:14250
    tls:
      insecure: true
  # otlp/jaeger:
  #   endpoint: jaeger:14250
  #   tls:
  #     insecure: true
  prometheus:
    endpoint: otel_collector:9464
  # azuremonitor:
  #   instrumentation_key: 95c2e742-92c5-47b4-9aff-2589d8592314


service:
  # A pipeline consists of a set of receivers, processors and exporters.
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [jaeger]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]

